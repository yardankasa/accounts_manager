# Telegram Bot (admin panel)
BOT_TOKEN=your_bot_token_here

# Telegram API: not used from here. Each login uses API_ID and API_HASH
# entered by the admin in the bot (per-account).

# MySQL (filled by scripts/install_mysql.sh)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=rezabots
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=rezabots

# Comma-separated Telegram user IDs allowed as admins (bootstrap)
ADMIN_IDS=123456789,987654321

# Bot username (without @) - accounts send /im_alive_ to this bot. Example: StarsRM_bot
BOT_USERNAME=StarsRM_bot

# Private channel ID for im_alive notifications (e.g. -1001234567890). Bot must be admin.
IM_ALIVE_CHANNEL_ID=-1002513510518

# API_ID and API_HASH for session status check (if accounts don't store per-account credentials)
# API_ID=
# API_HASH=
‌