## جریان کامل کاربر در Rezabots (نسخه تک‌سروره)

این سند یک نگاه «از بالا» به پروژه می‌دهد تا بعد از مدت طولانی دوری، خیلی سریع دوباره یاد بگیرید **چطور سیستم کار می‌کند، چه بخش‌هایی دارد، و مزایا / معایب معماری فعلی چیست**.

---

## ۱. تصویر کلی سیستم

- **هسته سیستم**: یک بات ادمین تلگرام (python-telegram-bot) که روی یک سرور اجرا می‌شود.
- **دیتابیس**: MySQL برای نگه‌داشتن اطلاعات ادمین‌ها، نود اصلی، اکانت‌ها، رویدادهای ورود و تنظیمات رفتار انسانی.
- **ورود اکانت‌ها**: با استفاده از Telethon روی **همان سرور** (دیگر خبری از نودهای SSH و لاگین راه‌دور نیست).
- **رفتار انسانی (Humantic)**: اسکریپت‌هایی که روی همین سرور، با بازه‌های تصادفی جوین/ترک و پیام خصوصی را شبیه‌سازی می‌کنند.

جریان کلی:

1. ادمین در `.env` تعریف می‌شود و بات را روی سرور اجرا می‌کنید.
2. ادمین در تلگرام `/admin` می‌فرستد و وارد پنل می‌شود.
3. از منوی «Account Loginer» اکانت‌های جدید را روی **همین سرور** وارد می‌کند.
4. از «📋 لیست اکانت‌ها» می‌تواند وضعیت سشن‌ها را چک کند، حذف کند، و تست `im_alive` بگیرد.
5. از «مدیریت رفتار انسانی» اجرای خودکار جوین/ترک/پیوی با بازه‌های تصادفی را مدیریت می‌کند.

---

## ۲. معماری ساده‌شده (فقط یک سرور، بدون SSH)

معماری نسخه فعلی، به صورت خلاصه:

```
┌──────────────────────────────────────────────┐
│                سرور فعلی                     │
│                                              │
│  ┌──────────────┐    ┌───────────────────┐   │
│  │  بات ادمین   │    │   Core / DB       │   │
│  │  (Telegram   │    │  - MySQL          │   │
│  │   Bot v21)   │───▶│  - accounts       │   │
│  │              │    │  - login_events   │   │
│  │ منوی فارسی   │    │  - humantic       │   │
│  └──────┬───────┘    └───────────────────┘   │
│         │                                     │
│         │  Telethon روی همین سرور            │
│         ▼                                     │
│  ┌─────────────────────┐                      │
│  │ سشن اکانت‌ها (files)│  data/session       │
│  └─────────────────────┘                      │
└──────────────────────────────────────────────┘

ادمین‌ها از تلگرام ↔ بات ادمین ↔ Core ↔ دیتابیس / سشن‌ها
همه چیز روی **یک سرور واحد** انجام می‌شود.
```

در نسخه‌های قبلی، یک سرور اصلی + چند نود SSH وجود داشت که بات از راه دور به آن‌ها وصل می‌شد. **الان این قسمت حذف شده** و هر مشتری در صورت نیاز، می‌تواند همین پروژه را روی سرورهای دیگر خودش، جداگانه اجرا کند.

---

## ۳. فلوهای اصلی از نگاه کاربر/ادمین

### ۳.۱. راه‌اندازی اولیه

1. **نصب وابستگی‌ها و دیتابیس** (طبق `README.md`):
   - ساخت venv و `pip install -e .`
   - اجرای اسکریپت نصب MySQL در صورت نیاز.
2. **تنظیم `.env`** در پوشه `src`:
   - `BOT_TOKEN`, `ADMIN_IDS`, `MYSQL_*`, و در صورت نیاز `PROXY_URL`.
3. اجرای بات:
   - `python main.py` یا اسکریپت `rezabots`.

نتیجه: بات جدول‌ها را می‌سازد، نود اصلی را ایجاد می‌کند و شروع به polling می‌کند.

### ۳.۲. ورود اکانت تلگرام (Account Loginer)

۱. ادمین در تلگرام دکمه `Account Loginer` را می‌زند.  
۲. بات:
   - نود اصلی را از دیتابیس می‌خواند.
   - محدودیت لاگین (۳ لاگین در ۲۴ ساعت، حداقل ۴ ساعت فاصله) را روی **همین سرور** چک می‌کند.
۳. اگر ظرفیت وجود داشته باشد، بات به ترتیب این اطلاعات را می‌گیرد:
   - `API_ID`
   - `API_HASH`
   - شماره تلفن (نرمال‌سازی: ایران یا سایر کشورها)
۴. Telethon روی همین سرور اجرا می‌شود:
   - کد پیامک‌شده را از ادمین می‌گیرد.
   - در صورت نیاز، رمز دو مرحله‌ای (2FA) را می‌پرسد.
۵. در صورت موفقیت:
   - سشن در مسیر `data/session/...` ذخیره می‌شود.
   - رکورد در جدول `accounts` + `login_events` ثبت می‌شود.
   - پیام موفقیت به ادمین ارسال می‌شود.

**نکته مهم**: دیگر هیچ اتصال SSH و لاگین راه‌دور برای نودهای دیگر وجود ندارد. تمام لاگین‌ها روی همین سرور انجام می‌شود.

### ۳.۳. مدیریت اکانت‌ها

از منوی «📋 لیست اکانت‌ها»:

- لیست همه اکانت‌های ثبت‌شده روی این سرور را می‌بینید.
- برای هر اکانت:
  - **حذف**: رکورد دیتابیس + فایل‌های سشن (فقط روی همین سرور) پاک می‌شود.
  - **📊 وضعیت**: Telethon روی همین سرور سشن را تست می‌کند و یک گزارش شامل:
    - فعال / غیرفعال بودن سشن
    - زمان ورود
    - نام نود (عملاً «نود اصلی» = همین سرور)
  - **👓 درخواست `im_alive`**: اگر BOT_USERNAME تنظیم شده باشد، اکانت یک `/start` و `/im_alive_$phone` به بات می‌فرستد و نتیجه به ادمین‌ها گزارش می‌شود.

### ۳.۴. وضعیت سرور (منوی «مدیریت نودها» در نسخه جدید)

در نسخه جدید، منوی «🖥 مدیریت نودها» به یک **داشبورد وضعیت سرور فعلی** تبدیل می‌شود:

- نمایش:
  - Load average (۱، ۵ و ۱۵ دقیقه اخیر)
  - درصد استفاده CPU
  - وضعیت RAM (استفاده‌شده / کل)
  - وضعیت دیسک (فضای استفاده‌شده / آزاد)
  - تعداد کل اکانت‌های ثبت‌شده
  - تعداد لاگین‌های انجام‌شده در ۲۴ ساعت گذشته روی همین سرور
  - آخرین زمان لاگین موفق
- دیگر **نمی‌توان نود جدید اضافه کرد یا نود را حذف کرد**؛ سیستم فقط یک نود منطقی دارد: «سرور فعلی».

### ۳.۵. رفتار انسانی (Humantic)

- از دکمه «مدیریت رفتار انسانی»:
  - روشن/خاموش کردن رفتار انسانی
  - تنظیم بازه اجرای اکشن‌ها (۴–۶، ۸–۱۲، ۲۴–۳۰ ساعت و ...)
  - تنظیم زمان ترک کانال/گروه
  - تنظیم خواب اکانت در صورت فلو
  - تنظیم خواب سیستم در صورت فلو چند اکانت
- یک job هر ۵ دقیقه وضعیت را چک می‌کند و در صورت رسیدن زمان، `run_all_accounts` را اجرا می‌کند.
- فقط اکانت‌های روی همین سرور استفاده می‌شوند (مطابق نسخه ساده‌شده تک‌سروره).

---

## ۴. مزایا و معایب معماری فعلی (بدون نود SSH)

### ۴.۱. مزایا

- **سادگی استقرار**:
  - فقط یک سرور لازم است.
  - نیازی به تنظیم SSH، پسورد، پورت و اسکریپت راه‌دور نیست.
- **امنیت بهتر**:
  - دیگر پسوردهای SSH نودهای دیگر در دیتابیس ذخیره نمی‌شود.
  - سطح حمله (attack surface) کمتر است، چون هیچ اتصال راه‌دور خودکار برقرار نمی‌شود.
- **دیباگ و مانیتورینگ ساده‌تر**:
  - همه‌چیز (بات، سشن‌ها، رفتار انسانی) روی یک سرور است؛ لاگ‌ها و خطاها در یک جا متمرکزند.
- **استقلال مشتری‌ها**:
  - هر مشتری می‌تواند همین پروژه را روی سرور خودش اجرا کند، بدون نیاز به اتصال به «سرور مرکزی شما».

### ۴.۲. معایب

- **حذف چندنودی مرکزی**:
  - دیگر نمی‌توان از یک بات مرکزی، چند سرور SSH را مدیریت کرد.
  - اگر این نیاز وجود داشته باشد، باید یا چند instance مجزا از بات را اجرا کنید، یا در آینده یک لایه orchestrator جداگانه طراحی کنید.
- **مقیاس‌پذیری افقی دستی**:
  - برای افزایش ظرفیت، به‌جای اضافه کردن نود SSH، باید پروژه را روی سرور دوم/سوم نصب و مستقل اجرا کنید.
  - جمع‌آوری وضعیت همه این سرورها به‌صورت یک‌جا در نسخه فعلی وجود ندارد.
- **انتقال از نسخه قدیمی**:
  - اگر قبلاً نودهای SSH تعریف کرده‌اید، در نسخه فعلی لاگین جدید روی آن‌ها انجام نمی‌شود و باید اکانت‌ها را روی سرور فعلی دوباره لاگین کنید.

---

## ۵. استفاده از پروژه روی سرورهای دیگر (بدون SSH مرکزی)

برای اینکه مشتری روی سرور خودش از پروژه استفاده کند:

1. این مخزن را روی سرور خودش کلون کند.
2. همان مراحل `README.md` برای نصب MySQL، ساخت venv و اجرای بات را انجام دهد.
3. `.env` مخصوص خودش را تنظیم کند (توکن بات، ادمین‌ها، دیتابیس آن سرور).
4. بات را اجرا کند و اکانت‌ها را وارد کند.

به این ترتیب، **هر سرور، یک instance مستقل از Rezabots** است؛ بات مرکزی برای مدیریت چند نود SSH دیگر وجود ندارد.

